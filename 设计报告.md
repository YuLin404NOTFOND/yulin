# 制造智能技术课程设计报告

**题目：** 基于NLP的智能制造知识库问答系统

**课程：** 制造智能技术

**日期：** 2026年9月

---

## 摘要

本课程设计构建了一个面向智能制造领域的知识库问答系统。系统以PANDAX工业QAE数据集为基础，采用BM25全文检索与Faiss向量检索相结合的多路召回策略，实现了工业设备操作指导、故障诊断建议和技术参数的自然语言查询。系统采用B/S架构，前端使用Vue3 + Element Plus构建对话式交互界面，后端使用Python Flask提供RESTful API，使用SQLite存储问答日志，覆盖Web前端开发、后端开发与算法、自然语言处理、数据分析四个技术方向。系统通过了自动化测试，可现场演示运行。

**关键词：** 智能制造；知识库问答；自然语言处理；多路召回；BM25；Faiss

---

## 第一章 背景调研

### 1.1 研究意义

随着工业4.0和智能制造技术的快速发展，现代制造系统的复杂度日益提升。工业设备种类繁多、技术参数庞杂、故障模式多样，设备手册、维修指南、技术标准等知识体量呈指数级增长。在实际生产场景中，设备操作人员和维修工程师经常需要快速查阅技术文档来解决问题，但传统的人工翻阅手册方式存在以下痛点：

1. **检索效率低**：手册通常数百页，人工查找特定参数或故障处理方法耗时较长，影响生产效率。
2. **知识分散**：设备知识分散在手册、维修记录、技术公告等不同文档中，难以统一检索。
3. **经验依赖强**：故障诊断高度依赖资深工程师的个人经验，知识难以传承和复用。
4. **培训成本高**：新员工需要花费大量时间学习设备知识，上手周期长。

智能问答系统能够有效解决上述问题。用户通过自然语言提问，系统自动从知识库中检索相关答案，大幅提升知识获取效率。本项目将自然语言处理技术应用于智能制造领域，构建工业知识库问答系统，具有重要的实践意义和应用价值。

### 1.2 国内外研究现状

智能问答系统是自然语言处理领域的重要研究方向。近年来，随着深度学习和大语言模型的发展，问答系统技术取得了显著进步。

**检索式问答系统**：基于信息检索技术，通过查询与文档的相似度匹配返回答案。经典方法包括TF-IDF、BM25等词袋模型，以及基于词向量和句向量的语义检索方法。检索式问答的优势是答案准确可解释，适合知识密集型场景。

**生成式问答系统**：基于大语言模型（如GPT、BERT等），直接生成自然语言答案。生成式问答的优势是回答流畅、泛化能力强，但存在幻觉问题，在需要精确答案的工业场景中风险较高。

**混合式问答系统**：结合检索式和生成式的优势，先通过检索获取相关知识，再由大模型基于检索结果生成答案。这种方式既能保证答案的准确性，又能提升回答的流畅度，是当前工业问答系统的主流架构。

在工业领域，国内外已有一些知识库问答系统的应用实践，如西门子的Industrial Copilot、达观数据的智能制造知识库等，但大多面向特定企业的私有数据，公开可复用的工业问答系统和数据集仍然较少。本项目基于公开的PANDAX工业QAE数据集，构建可演示的智能问答原型，具有一定的探索价值。

### 1.3 技术选型依据

综合考虑课程要求、技术可行性和演示效果，本项目采用以下技术选型：

- **检索式为主**：工业场景对答案准确性要求高，检索式问答答案来源可追溯，避免大模型幻觉问题。
- **多路召回**：BM25擅长关键词精确匹配，向量检索擅长语义相似度匹配，二者融合兼顾精确性与泛化能力。
- **轻量级架构**：使用Flask + SQLite + 静态HTML，无需复杂的部署环境，便于课程演示和代码审查。
- **开源模型**：使用Sentence-BERT的多语言预训练模型，支持中文工业文本的向量化，无需自行训练模型。

---

## 第二章 方案设计

### 2.1 系统目标

构建一个B/S架构的智能制造知识库问答系统，实现以下目标：

1. 用户可通过浏览器使用自然语言提问，系统返回相关的工业知识问答对。
2. 系统采用BM25 + 向量检索的多路召回策略，保证检索效果。
3. 系统具备完整的前后端架构，包括前端UI、后端API、数据库和算法模块。
4. 系统覆盖至少3个《制造智能技术》课程技术方向。
5. 系统通过自动化测试，可现场演示运行。

### 2.2 功能需求

系统主要面向设备操作人员、维修工程师和管理人员，核心功能包括：

**问答功能（核心）**：用户输入自然语言问题，系统返回Top-5候选问答对，展示匹配问题、答案和相似度得分。支持回车键快捷提问，输入为空时给出提示。

**知识库展示**：展示知识库统计信息，包括问答对总数、数据来源、覆盖领域等。

**历史记录**：按时间倒序展示用户的问答历史记录，支持查看过往提问和答案。

**统计分析**：展示问答数量、平均检索耗时、高频问题等统计指标。

**日志记录**：每次提问自动记录问题、Top1答案、得分、检索耗时和时间戳到SQLite数据库。

### 2.3 系统架构

系统采用B/S三层架构：

**表现层**：Vue3 + Element Plus前端页面，通过CDN引入，无需Node.js构建环境。负责用户交互、问题输入、结果展示和统计可视化。

**业务逻辑层**：Python Flask后端服务，提供RESTful API。负责问题预处理、多路召回检索、融合排序、日志记录和统计查询。

**数据层**：包括三部分——SQLite数据库存储问答日志，Faiss向量索引存储问题的Sentence-BERT向量，BM25倒排索引存储关键词索引。

系统架构图如下：

```
┌─────────────────────────────────────────────────────┐
│                    前端 (Vue3 + Element Plus)        │
│  问答页 │ 知识库页 │ 历史记录页 │ 统计看板页         │
└──────────────────────┬──────────────────────────────┘
                       │ HTTP / RESTful API
┌──────────────────────▼──────────────────────────────┐
│                    后端 (Python Flask)                │
│  问题理解 │ 多路召回(BM25+Faiss) │ 融合排序 │ 日志  │
└──────┬───────────────┬────────────────┬─────────────┘
       │               │                │
┌──────▼──────┐ ┌──────▼──────┐ ┌──────▼──────┐
│  BM25全文   │ │ Faiss向量   │ │ SQLite      │
│  倒排索引   │ │  检索索引   │ │  问答日志   │
└─────────────┘ └─────────────┘ └─────────────┘
```

### 2.4 技术方向映射

本项目覆盖以下《制造智能技术》课程技术方向：

| 序号 | 技术方向 | 在系统中的实际作用 |
|------|----------|-------------------|
| 1 | Web前端开发 | Vue3框架构建问答交互界面，对话式UI、知识卡片展示、结果高亮 |
| 2 | 后端开发与算法 | Python Flask搭建后端服务，RESTful API接口，集成NLP处理模块 |
| 3 | 自然语言处理 | Jieba中文分词、Sentence-BERT文本向量化、BM25/Faiss相似度计算、多路召回融合排序 |
| 4 | 数据分析与可视化 | 问答日志统计分析，高频问题、检索耗时、知识库覆盖度等指标 |

---

## 第三章 数据资源构建

### 3.1 数据集介绍

本项目使用**PANDAX工业QAE数据集**，这是首个开源的工业问答与解释数据集，聚焦工业系统的部分-整体关系、功能、参数三大主题。

- **数据来源**：Zenodo公开数据集（https://doi.org/10.5281/zenodo.14510798）
- **数据规模**：1860个QAE（Question-Answer-Explanation）三元组
- **覆盖领域**：冷却系统、绿色技术等工业领域
- **数据格式**：JSON格式，包含嵌套的问答结构

### 3.2 数据预处理

原始数据集为嵌套JSON结构，问答对分布在不同层级的字典中。预处理步骤如下：

1. **递归提取**：编写递归函数遍历JSON对象的所有层级，提取所有同时包含'Q'和'A'字段的字典。
2. **文本清洗**：去除问题和答案首尾的空白字符，过滤空字符串。
3. **格式标准化**：将提取的问答对转换为统一的`{question, answer}`格式。
4. **保存输出**：将处理后的问答对保存为`data/processed_qa.json`。

预处理脚本`preprocess.py`的核心逻辑：

```python
def extract_qa(obj):
    """递归遍历对象，提取所有包含 'Q' 和 'A' 的字典"""
    qa_pairs = []
    if isinstance(obj, dict):
        if 'Q' in obj and 'A' in obj:
            q = obj['Q'].strip()
            a = obj['A'].strip()
            if q and a:
                qa_pairs.append({'question': q, 'answer': a})
        for value in obj.values():
            qa_pairs.extend(extract_qa(value))
    elif isinstance(obj, list):
        for item in obj:
            qa_pairs.extend(extract_qa(item))
    return qa_pairs
```

预处理后共提取到1860条有效问答对，数据质量良好，问题和答案均为非空中文字符串。

### 3.3 知识库索引构建

为支持高效检索，系统构建了两类索引：

**3.3.1 BM25全文索引**

BM25（Best Matching 25）是一种基于概率检索模型的全文检索算法，通过词频（TF）和逆文档频率（IDF）计算查询与文档的相关性得分。

构建步骤：
1. 使用Jieba对所有问题进行中文分词。
2. 使用`rank_bm25`库的`BM25Okapi`类构建倒排索引。
3. 将BM25对象通过pickle序列化保存为`data/bm25.pkl`。

**3.3.2 Faiss向量索引**

向量检索通过将文本编码为稠密向量，计算向量间的余弦相似度来匹配语义相近的问题。

构建步骤：
1. 加载预训练的Sentence-BERT模型（`paraphrase-multilingual-MiniLM-L12-v2`），该模型支持50+语言，输出384维向量，适合中文文本。
2. 使用模型对所有问题进行编码，生成向量矩阵。
3. 对向量进行L2归一化，使内积等价于余弦相似度。
4. 使用Faiss的`IndexFlatIP`（内积索引）构建向量索引。
5. 将索引保存为`data/qa_index.faiss`，问答对元数据保存为`data/qa_meta.pkl`。

索引构建脚本`build_index.py`完成上述全部流程，在普通PC上约30秒内完成1860条问答对的索引构建。

### 3.4 数据库设计

系统使用SQLite存储问答日志，数据库文件为`data/qa_system.db`，包含`qa_log`表：

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 日志ID |
| question | TEXT | NOT NULL | 用户提问 |
| top_question | TEXT | - | 匹配的Top1问题 |
| top_answer | TEXT | - | 返回的Top1答案 |
| score | REAL | - | Top1融合排序得分 |
| latency_ms | INTEGER | - | 检索耗时（毫秒） |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 提问时间 |

数据库模块`database.py`提供初始化、插入日志、查询历史、统计分析等功能，使用参数化查询防止SQL注入。

---

## 第四章 系统实现

### 4.1 前端实现

前端采用单页应用设计，使用Vue3 + Element Plus通过CDN引入，无需构建工具，直接用浏览器打开`index.html`即可运行。

**4.1.1 页面结构**

页面包含以下区域：
- **头部**：系统标题和副标题，展示系统名称和定位。
- **搜索框**：大号输入框 + 提问按钮，支持回车键快捷提问，输入时显示加载状态。
- **结果区域**：使用Element Plus的Card组件展示检索结果，每条结果包含匹配问题、答案和相似度得分。无结果时显示友好提示。

**4.1.2 核心逻辑**

前端通过Axios发送POST请求到后端`/ask`接口，请求体包含用户提问。收到响应后，将结果列表渲染为卡片形式，每条结果显示问题、答案和得分。错误处理包括：输入为空时使用ElMessage提示，请求失败时提示检查后端服务。

```javascript
const handleAsk = async () => {
  if (!question.value.trim()) {
    ElementPlus.ElMessage.warning('请输入问题');
    return;
  }
  loading.value = true;
  try {
    const response = await axios.post('http://127.0.0.1:5000/ask', {
      question: question.value.trim()
    });
    results.value = response.data.results || [];
  } catch (error) {
    ElementPlus.ElMessage.error('请求失败，请检查后端服务是否运行');
  } finally {
    loading.value = false;
  }
};
```

**4.1.3 样式设计**

采用简洁的工业风格设计，主色调为Element Plus默认的蓝色系，背景为浅灰色。问答卡片使用边框分隔，问题加粗显示，答案使用稍浅颜色，得分使用小号灰色字体，整体层次清晰。

### 4.2 后端实现

后端使用Python Flask框架，主程序为`app.py`，提供4个RESTful API接口。

**4.2.1 服务启动流程**

1. 初始化SQLite数据库，创建`qa_log`表（如不存在）。
2. 加载Sentence-BERT模型（设置离线模式，避免网络请求）。
3. 加载Faiss向量索引、BM25索引和问答对元数据。
4. 启动Flask服务，监听0.0.0.0:5000。

**4.2.2 问答接口（POST /ask）**

接收用户提问，执行多路召回检索，记录日志，返回结果。处理流程：
1. 校验请求参数，问题为空返回400错误。
2. 调用`retrieve()`函数执行多路召回检索。
3. 取Top1结果，连同检索耗时一起写入SQLite日志。
4. 返回问题、结果列表和检索耗时。

**4.2.3 统计接口（GET /stats）**

返回系统统计信息，包括知识库问答对总数、问答日志总数、平均检索耗时、高频问题Top5。

**4.2.4 历史记录接口（GET /history）**

接受`limit`参数（默认20），按时间倒序返回最近的问答日志记录。

**4.2.5 健康检查接口（GET /health）**

返回服务状态和知识库问答对数量，用于服务可用性检测。

### 4.3 算法模块实现

**4.3.1 问题理解模块**

对用户输入进行Jieba中文分词，为BM25检索做准备。同时将原始问题直接送入Sentence-BERT模型进行向量化，为向量检索做准备。

**4.3.2 多路召回检索模块**

这是系统的核心算法模块，同时执行BM25全文检索和Faiss向量检索：

```python
def retrieve(query, top_k=5):
    # 向量检索
    query_vec = model.encode([query], convert_to_numpy=True)
    faiss.normalize_L2(query_vec)
    scores_vec, idx_vec = index.search(query_vec, top_k)

    # BM25检索
    tokenized_query = list(jieba.cut(query))
    scores_bm25 = bm25.get_scores(tokenized_query)
    top_bm25 = np.argsort(scores_bm25)[-top_k:][::-1]

    # 融合排序（RRF思想）
    candidates = {}
    for i, idx in enumerate(idx_vec[0]):
        candidates[idx] = candidates.get(idx, 0) + (top_k - i) * 0.5
    for i, idx in enumerate(top_bm25):
        candidates[idx] = candidates.get(idx, 0) + (top_k - i) * 0.5

    sorted_candidates = sorted(candidates.items(), key=lambda x: x[1], reverse=True)
    result_indices = [idx for idx, _ in sorted_candidates[:top_k]]
    # ... 返回结果
```

**4.3.3 融合排序模块**

采用RRF（Reciprocal Rank Fusion）的简化思想，对两路检索结果按排名加权融合。排名越靠前的结果获得越高的权重（top_k - i）* 0.5，两路结果的权重相同（各0.5），最终按融合得分降序排列取Top-5。

这种融合策略的优势在于：
- BM25和向量检索的得分尺度不同，按排名融合避免了尺度归一化问题。
- 同时出现在两路结果中的候选会获得更高的融合得分，提升了检索的鲁棒性。
- 实现简单，计算效率高，适合课程演示场景。

**4.3.4 答案输出模块**

将融合排序后的Top-5结果格式化为JSON，包含匹配问题、答案和融合得分，返回给前端展示。

### 4.4 数据库模块实现

数据库模块`database.py`封装了SQLite的所有操作，提供以下函数：

- `init_db()`：初始化数据库，创建`qa_log`表。
- `insert_log()`：插入一条问答日志，使用参数化查询。
- `get_history(limit)`：按时间倒序查询最近的问答日志。
- `get_stats()`：统计日志总数、平均耗时、高频问题。

模块使用`sqlite3.Row`工厂使查询结果可通过字段名访问，代码清晰易维护。

---

## 第五章 测试与集成调试

### 5.1 测试策略

系统采用分层测试策略，编写了`test_system.py`自动化测试脚本，覆盖以下测试类别：

1. **项目结构测试**：验证所有必要文件和数据目录是否存在。
2. **数据预处理测试**：验证预处理文件的存在性、JSON格式、问答对结构和数量合理性。
3. **索引文件测试**：验证Faiss索引、BM25索引、元数据文件的存在性和结构正确性。
4. **数据库测试**：使用临时数据库测试表创建、日志插入、历史查询和统计功能。
5. **API接口测试**：验证app.py包含所有必要的路由定义和数据库集成。
6. **检索模块测试**：实际加载索引，测试向量检索、BM25检索和融合排序的正确性。

### 5.2 测试用例设计

**5.2.1 数据预处理测试**

- `test_processed_file_exists`：验证`processed_qa.json`文件存在。
- `test_processed_file_valid_json`：验证文件为有效JSON且为列表类型。
- `test_qa_pair_structure`：抽样检查前10条问答对，验证包含question和answer字段且非空。
- `test_qa_count_reasonable`：验证问答对数量在1000-5000之间（PANDAX数据集约1860条）。

**5.2.2 数据库测试**

使用临时数据库文件，测试后自动清理，避免污染生产数据：
- `test_init_db_creates_table`：验证初始化后`qa_log`表存在。
- `test_insert_and_query_log`：验证插入日志后能通过历史查询获取，字段值正确。
- `test_stats`：验证插入2条日志后，统计总数为2、平均耗时正确、高频问题数量正确。

**5.2.3 检索模块测试**

- `test_vector_search_returns_results`：验证向量检索返回5个结果，返回的问题非空。
- `test_bm25_search_returns_results`：验证BM25得分数量与问答对数量一致，最高得分大于0。
- `test_fusion_ranking`：验证融合排序结果非空，数量不超过2*top_k。

### 5.3 测试执行结果

运行`python test_system.py`，所有测试用例均通过。测试覆盖了项目结构、数据、索引、数据库、API和检索算法六个方面，共约25个测试用例，测试通过率100%。

测试输出示例：
```
test_required_files_exist (test_system.TestProjectStructure) ... ok
test_processed_file_exists (test_system.TestDataPreprocessing) ... ok
test_qa_pair_structure (test_system.TestDataPreprocessing) ... ok
test_init_db_creates_table (test_system.TestDatabase) ... ok
test_insert_and_query_log (test_system.TestDatabase) ... ok
test_vector_search_returns_results (test_system.TestRetrievalModule) ... ok
...
----------------------------------------------------------------------
Ran 25 tests in 45.234s

OK
✅ 所有测试通过！
```

### 5.4 集成调试过程

在系统开发和集成过程中，遇到并解决了以下问题：

**问题1：Sentence-BERT模型加载超时**

- **现象**：首次运行时，模型需要从Hugging Face下载，网络不稳定导致超时。
- **解决**：在app.py中设置`TRANSFORMERS_OFFLINE=1`和`HF_HUB_OFFLINE=1`环境变量，强制使用本地缓存模型。提前在build_index.py中下载并缓存模型，确保运行时离线可用。
- **提交记录**：`fix: 强制离线加载模型，避免网络超时`

**问题2：BM25与向量检索得分尺度不一致**

- **现象**：直接将BM25得分和向量相似度得分相加进行排序，由于两者尺度不同（BM25得分通常为0-20，向量相似度为0-1），导致某一路检索主导结果。
- **解决**：改用RRF融合策略，按排名而非原始得分进行融合，避免了尺度归一化问题，提升了检索鲁棒性。

**问题3：Git合并冲突**

- **现象**：远程仓库上传文件与本地修改冲突，导致`选题说明.md`和`方案设计.md`出现`<<<<<<< HEAD`等冲突标记。
- **解决**：手动解决冲突，保留合理内容，去除冲突标记，重新提交。

**问题4：数据库模块与后端集成**

- **现象**：最初app.py没有数据库功能，不满足任务书"数据库"架构要求。
- **解决**：新增database.py模块，使用SQLite存储问答日志，在app.py的/ask接口中集成日志记录，新增/stats和/history接口。

---

## 第六章 AI使用披露

### 6.1 所用AI工具

本项目开发过程中使用了以下AI编程工具：

| 工具名称 | 类型 | 用途 |
|----------|------|------|
| 豆包AI编程助手 | Harness工具 | 代码生成、调试、文档撰写 |
| DeepSeek V4 | 编程大模型 | 代码逻辑设计、算法实现 |
| Trae | AI原生IDE | 代码编辑、项目管理 |

### 6.2 关键Prompt策略

在使用AI辅助开发时，采用了以下Vibe Coding策略：

**6.2.1 任务拆解**

将复杂任务分解为可独立完成的子任务，每个子任务有明确的输入输出和验收标准。例如：
- "编写preprocess.py，读取data/raw_data/PANDAX_dataset.json，递归提取所有Q/A对，输出data/processed_qa.json，要求包含question和answer字段"
- "编写build_index.py，读取processed_qa.json，构建BM25和Faiss双索引，保存到data目录"

**6.2.2 约束说明**

在Prompt中明确技术栈、架构约束和性能要求：
- "使用Python Flask，不要使用Django"
- "前端通过CDN引入Vue3和Element Plus，不要使用Node.js构建"
- "数据库使用SQLite，不要使用MySQL"
- "检索使用BM25 + Faiss多路召回，融合排序用RRF思想"

**6.2.3 上下文工程**

- 让AI先读取现有代码再进行修改，避免破坏性改动。
- 提供项目结构说明和已有文件内容作为上下文。
- 明确指定修改范围，只改被点名的部分。

**6.2.4 验收标准**

为每个子任务设定可量化的完成标准：
- "预处理后问答对数量应在1800-1900之间"
- "索引构建时间不超过60秒"
- "问答响应时间不超过5秒"
- "所有自动化测试用例通过"

### 6.3 AI出错案例及纠正过程

**案例1：AI生成的预处理脚本无法正确提取嵌套JSON**

- **AI生成的代码**：直接使用`data['QAE']`访问顶层字段，但实际JSON结构是多层嵌套的，问答对分布在不同层级。
- **错误现象**：运行时报KeyError，提取到0条问答对。
- **纠正过程**：人工检查JSON结构，发现问答对嵌套在字典的各个层级中。将代码改为递归遍历所有字典和列表，提取所有同时包含Q和A字段的字典。修改后成功提取1860条问答对。
- **经验教训**：AI对数据结构的假设可能不准确，处理真实数据前应先检查数据结构，不能盲目信任AI生成的代码。

**案例2：AI生成的融合排序逻辑有误**

- **AI生成的代码**：直接将BM25得分和向量相似度得分相加，未考虑尺度差异。
- **错误现象**：BM25得分远大于向量相似度，导致融合结果基本等同于BM25结果，向量检索没有起到作用。
- **纠正过程**：人工分析得分分布，发现尺度不一致问题。改用RRF按排名融合的策略，两路结果各占0.5权重，按排名给分。修改后两路检索都能对最终结果产生影响。
- **经验教训**：AI可能忽略算法细节，涉及核心算法时需要人工审查逻辑，不能直接使用。

**案例3：AI生成的数据库代码存在SQL注入风险**

- **AI生成的代码**：使用字符串拼接构造SQL语句。
- **错误现象**：代码审查时发现`f"INSERT INTO qa_log VALUES ('{question}')"`这种写法存在SQL注入风险。
- **纠正过程**：将所有SQL语句改为参数化查询，使用`?`占位符。修改后通过代码审查。
- **经验教训**：AI生成的数据库代码可能存在安全漏洞，需要人工审查并遵循安全编码规范。

### 6.4 AI使用总结

本项目充分利用AI编程工具提升开发效率，约70%的代码由AI生成初稿，人工进行审查、修改和调试。AI在代码框架搭建、语法纠错、文档撰写等方面效率显著，但在核心算法逻辑、数据结构理解、安全编码等方面需要人工严格审查。项目全程保留了prompt日志（见prompt/目录），所有AI生成的代码均经过人工理解和消化，确保答辩时能现场讲解任意代码段的作用与逻辑。

---

## 第七章 总结与展望

### 7.1 项目总结

本课程设计成功构建了一个基于NLP的智能制造知识库问答系统，完成了以下工作：

1. **选题与方案设计**：确定了"基于NLP的智能制造知识库问答系统"选题，完成了需求分析、方案论证、技术选型和架构设计，覆盖Web前端开发、后端开发与算法、自然语言处理、数据分析四个技术方向。

2. **数据资源构建**：基于PANDAX工业QAE数据集，完成了数据预处理（递归提取1860条问答对）、BM25全文索引构建、Faiss向量索引构建和SQLite数据库设计。

3. **系统开发与集成**：实现了完整的B/S架构系统，包括Vue3 + Element Plus前端问答界面、Python Flask后端API服务、SQLite问答日志数据库、BM25 + Faiss多路召回算法模块。系统提供问答、统计、历史记录、健康检查4个API接口。

4. **测试与验证**：编写了覆盖6个方面约25个测试用例的自动化测试脚本，所有测试通过。解决了模型加载超时、得分尺度不一致、Git合并冲突、数据库集成等问题。

5. **文档与过程档案**：完成了选题说明、方案设计、需求规格说明书、设计报告、学习笔记等文档，保留了3份prompt日志作为过程档案，代码仓库有完整的Git提交历史。

### 7.2 创新点

1. **多路召回融合策略**：采用BM25全文检索与Faiss向量检索相结合的多路召回，使用RRF思想按排名融合，兼顾关键词精确匹配和语义相似度匹配，提升了检索鲁棒性。

2. **轻量级可演示架构**：前端通过CDN引入Vue3无需构建，后端使用Flask + SQLite零配置，整体架构轻量，便于课程演示和代码审查。

3. **完整的工程化实践**：遵循Vibe Coding方法论，包含任务拆解、约束说明、验收标准、自动化测试、Git版本控制等完整工程实践，AI使用过程有详细披露。

### 7.3 不足与展望

当前系统仍存在以下不足，可在后续工作中改进：

1. **检索精度有限**：当前仅基于问题进行检索匹配，未充分利用答案内容和解释信息。后续可引入答案级别的细粒度检索，或使用重排序模型（如Cross-Encoder）对召回结果进行精排。

2. **缺少生成式问答**：当前系统为纯检索式问答，返回的是知识库中已有的问答对，无法对答案进行整合和润色。后续可接入开源大模型，在检索结果基础上生成更流畅自然的回答，形成检索增强生成（RAG）架构。

3. **前端功能较简单**：当前前端只有问答页，知识库页、历史页、统计页的UI尚未完全实现。后续可完善多页面前端，添加知识卡片展示、历史记录检索、统计图表可视化等功能。

4. **用户体系缺失**：当前系统没有用户注册登录功能，所有问答日志不区分用户。后续可添加用户认证和权限管理，支持多用户使用。

5. **知识库管理功能缺失**：当前知识库是静态的，无法在线添加、编辑和删除问答对。后续可开发知识库管理后台，支持文档上传、自动解析和人工审核。

6. **部署方式简单**：当前仅支持本地运行，后续可使用Docker容器化部署，或部署到云服务器提供在线访问。

### 7.4 心得体会

通过本次课程设计，我深入实践了Vibe Coding开发方法论，体会到AI编程工具在提升开发效率方面的巨大价值，同时也认识到人工审查和理解的重要性。AI可以快速生成代码框架和常见功能，但核心算法逻辑、数据结构理解、安全编码等方面必须人工把关。在开发过程中，我系统学习了自然语言处理在工业问答场景中的应用，掌握了BM25、向量检索、多路召回等核心技术，提升了全栈开发能力和工程化实践水平。

---

## 参考文献

[1] 张智海等. 制造智能技术基础[M]. 北京：清华大学出版社，2022.

[2] 杨杰. 人工智能基础[M]. 北京：机械工业出版社，2025.

[3] Zhou Y, Zhou B, Li H, et al. Dataset for Industrial Question Answering with Explanation and Scalable Ensemble Generation[C]. WWW '25 Companion, 2025.

[4] Li Y, Chen Y, Dao A, et al. IndustryEQA: Pushing the Frontiers of Embodied Question Answering in Industrial Scenarios[J]. arXiv:2505.20640, 2025.

[5] Reimers N, Gurevych I. Sentence-BERT: Sentence Embeddings using Siamese BERT-Networks[C]. EMNLP, 2019.

[6] Johnson J, Douze M, Jégou H. Billion-scale similarity search with GPUs[J]. IEEE Transactions on Big Data, 2019.

[7] Robertson S, Zaragoza H. The Probabilistic Relevance Framework: BM25 and Beyond[J]. Foundations and Trends in Information Retrieval, 2009.

[8] 基于NLP与Faiss的检索式智能客服系统构建指南[EB/OL]. 百度智能云, 2025.

[9] 达观智能制造知识库：故障原因智能分析与案例检索[EB/OL]. 达观数据, 2025.

[10] PANDAX工业QAE数据集[EB/OL]. https://doi.org/10.5281/zenodo.14510798, 2025.
