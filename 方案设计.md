# 方案设计

## 一、功能需求分析

系统主要面向设备操作人员、维修工程师和管理人员。

**用户端功能：**
- 注册登录
- 自然语言提问
- 查看问答结果（多路召回结果展示）
- 对答案进行满意度评价
- 查看历史问答记录

**管理端功能：**
- 知识库文档的上传与管理
- 问答对的人工审核与标注
- 知识库版本管理
- 问答统计看板查看

**核心业务流程：**
用户输入自然语言问题 → 问题预处理与意图识别 → 知识库多路检索匹配（BM25 + 向量检索）→ 答案融合排序 → 结果返回与展示 → 问答日志记录

## 二、方案论证

### 2.1 问答技术选型

采用**检索式问答为主、生成式问答为辅**的混合架构。

- **检索式部分**：使用BM25倒排索引与向量检索相结合的多路召回策略，保证答案的准确性和可解释性。BM25擅长关键词精确匹配，向量检索（Sentence-BERT）擅长语义相似度匹配，二者融合可兼顾精确性与语义泛化能力。
- **生成式部分**：预留大模型接口，可使用开源大模型进行答案的精炼与润色（当前版本以检索式为主，确保系统稳定可演示）。

### 2.2 数据集选型

选用**PANDAX工业QAE数据集**——首个开源工业问答与解释数据集，聚焦工业系统的部分-整体关系、功能、参数三大主题，包含1860个QAE三元组。该数据集与课程要求的制造/工业场景高度匹配，且为公开数据集，无需企业真实数据。

### 2.3 前端框架选型

选用**Vue3搭配Element Plus**。Vue3的组合式API便于状态管理，Element Plus提供丰富的UI组件，对话式界面设计提升用户体验。采用CDN引入方式，无需Node.js构建环境，降低部署复杂度。

### 2.4 后端框架选型

采用**Python Flask**。Flask轻量灵活，便于集成各类NLP库（jieba、sentence-transformers、faiss、rank-bm25）和机器学习框架，RESTful API开发效率高。

### 2.5 数据存储选型

- **SQLite**：轻量级关系型数据库，存储用户信息和问答日志，零配置，满足课程设计演示需求。
- **Faiss**：Facebook开源的向量检索库，存储知识库向量索引，支持高效相似度搜索。
- **BM25索引（pickle序列化）**：存储倒排索引，支持关键词全文检索。

## 三、方案规划

### 3.1 技术栈

| 层级 | 技术选型 |
|------|---------|
| 前端 | Vue3、Element Plus、Axios |
| 后端 | Python Flask、RESTful API |
| 数据库 | SQLite 3（用户/日志） |
| 检索引擎 | BM25（全文检索）+ Faiss（向量检索） |
| NLP框架 | Sentence-BERT（文本向量化）、Jieba（中文分词） |
| 版本控制 | Git |

### 3.2 前端UI界面规划

包含四个主要页面：

1. **问答页**：对话式界面，用户输入问题后以聊天气泡形式展示问答过程，答案中高亮显示关键信息，显示相似度得分。
2. **知识库页**：展示当前知识库的文档列表、覆盖的领域和统计信息（问答对总数、领域分布）。
3. **历史页**：按时间线展示用户的问答记录，支持按关键词检索历史问题。
4. **统计页**：展示问答数量趋势、高频问题TOP榜、知识库覆盖度等指标。

### 3.3 后端算法设计

问答流程分为四个核心模块：

1. **问题理解模块**：对用户输入进行分词（jieba）、文本清洗，为后续检索做准备。
2. **知识检索模块**：采用多路召回策略，同时使用BM25倒排索引和向量相似度检索（Sentence-BERT + Faiss），从知识库中召回候选答案。
3. **答案排序模块**：对两路召回结果进行融合排序（RRF算法思想），选出最优答案。
4. **答案生成模块**：对选中的答案进行格式化输出，记录问答日志。

## 四、技术路线

整体流程分为七个步骤：

1. **第一步**：下载并预处理PANDAX数据集，构建初始知识库（preprocess.py）。
2. **第二步**：对知识库问答对进行文本清洗、向量化，建立BM25和Faiss双索引（build_index.py）。
3. **第三步**：设计并实现问题理解模块（分词、文本清洗）。
4. **第四步**：实现多路召回检索模块（BM25 + 向量检索）。
5. **第五步**：实现答案融合排序与输出模块。
6. **第六步**：开发后端业务逻辑和API接口（Flask），集成SQLite日志存储。
7. **第七步**：开发前端页面并与后端联调，完成系统测试与部署。

## 五、计划安排

| 阶段 | 时间 | 任务 |
|------|------|------|
| 1 | 8月26日-8月27日 | 完成选题与方案设计文档，初始化代码仓库 |
| 2 | 8月28日-8月30日 | 完成数据集的下载、预处理和知识库索引构建 |
| 3 | 8月31日-9月2日 | 完成问题理解模块和检索模块的开发 |
| 4 | 9月3日-9月5日 | 完成答案排序与生成模块，后端API接口，数据库集成 |
| 5 | 9月6日-9月9日 | 完成前端所有页面搭建，包括问答页、知识库页、历史页和统计页 |
| 6 | 9月10日-9月11日 | 完成前后端联调测试，优化问答效果，撰写设计报告，准备答辩 |

## 六、参考文献

[1] Zhou Y, Zhou B, Li H, et al. Dataset for Industrial Question Answering with Explanation and Scalable Ensemble Generation[C]. WWW '25 Companion, 2025.

[2] Li Y, Chen Y, Dao A, et al. IndustryEQA: Pushing the Frontiers of Embodied Question Answering in Industrial Scenarios[J]. arXiv:2505.20640, 2025.

[3] 基于NLP与Faiss的检索式智能客服系统构建指南[EB/OL]. 百度智能云, 2025.

[4] 基于KooSearch和DeepSeek构建知识问答[EB/OL]. 华为云, 2026.

[5] 达观智能制造知识库：故障原因智能分析与案例检索[EB/OL]. 达观数据, 2025.
